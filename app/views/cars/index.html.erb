<% @cars.each do |car| %>
  <%= car.name %> has something.

<!--   <%= link_to 'show', planet %>
  <%= link_to 'edit', edit_planet_path(planet) %>
  <%= link_to 'delete', planet, method: :delete, data: { confirm: 'Are you sure?'} %> -->

  <% end %>